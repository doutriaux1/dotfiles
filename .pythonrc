import readline
# import rlcompleter
readline.parse_and_bind("tab: complete")
import sys
import os
import vtk
renWin = vtk.vtkRenderWindow()
ren = vtk.vtkRenderer()
renWin.AddRenderer(ren)
a = vtk.vtkActor()
m = vtk.vtkPolyDataMapper()

import cdms2
try:
    import cdat_info
    pth = cdat_info.get_sampledata_path()
except:
    pth = sys.prefix
fnm = os.path.join(pth, 'clt.nc')
fnmirr = os.path.join(pth, 'sampleGenGrid3.nc')
fnmcurv = os.path.join(pth, 'sampleCurveGrid4.nc')
f = cdms2.open(fnm)
s = f("clt")
import cdutil
cdutil.times.setTimeBoundsMonthly(s)
import genutil  # noqa`

import numpy  # noqa
import cdtime  # noqa
import vcs
# from PyQt4 import QtCore, QtGui
x = vcs.init()
iso = x.createisofill()
T = x.createtemplate()


def Classes(obj, firsttime=True):
    if firsttime:
        obj = obj.__class__
    res = set()
    for c in obj.__bases__:
        res.add(c)
        res.update(Classes(c, False))
    return res

f = cdms2.open(fnm)
s = f('clt')
t = s.getTime()
tc = t.asComponentTime()
